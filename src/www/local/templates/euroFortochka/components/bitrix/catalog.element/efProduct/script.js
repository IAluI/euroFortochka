$(document).ready(() => {
  var productImages = new Swiper('.Product-MainImages', {
    navigation: {
      nextEl: '.swiper-button-next',
      prevEl: '.swiper-button-prev',
    },
    slidesPerView: 1,
  });
});